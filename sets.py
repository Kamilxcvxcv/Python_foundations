# set = collection which is unordered, unindexed. No duplicate values

utensils = {"fork","spoon","knife"}
dishes = {"bowl","plate","cup","knife"}
# methods of sets

# utensils.add("napkin")  # adds to set
# utensils.remove("fork")  # removes from set
# utensils.clear()  # removes all elements from set
# utensils.update(dishes)  # adds all elements found in the dishes set to utensils set
# dinner_table = utensils.union(dishes)  # creates new set by adding two other sets
# for x in dinner_table:  # prints new set
#     print(x)
# compares what on set have and other don't
# print(utensils.difference(dishes))  # prints what utensils have that dishes don't
# print(utensils.intersection(dishes))  # prints elements that they have in common

# for x in utensils:
#     print(x)  # sets are unordered as well as unindexed so every time you print it
#     # the order of printing would be different
